import React from 'react';

const ClickAnnouncer = () => {
    // Completely disabled to prevent React Virtual DOM crashes with browser extensions 
    // that modify dynamically added/removed DOM nodes (like translation tools).
    return null;
};

export default ClickAnnouncer;
